/* 项目页面样式 */
.page-container {
  min-height: 100vh;
  background: linear-gradient(135deg, #0f172a 0%, #1e293b 100%);
  position: relative;
  overflow-x: hidden;
}

/* 背景效果 */
.absolute-bg {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  pointer-events: none;
}

.bg-gradient-1 {
  position: absolute;
  top: -10%;
  right: -10%;
  width: 300rpx;
  height: 300rpx;
  border-radius: 50%;
  background: linear-gradient(135deg, rgba(22, 163, 74, 0.3), rgba(13, 148, 136, 0.2));
  filter: blur(40rpx);
  animation: pulse 3s ease-in-out infinite;
}

.bg-gradient-2 {
  position: absolute;
  bottom: -5%;
  left: -5%;
  width: 200rpx;
  height: 200rpx;
  border-radius: 50%;
  background: linear-gradient(135deg, rgba(13, 148, 136, 0.3), rgba(22, 163, 74, 0.2));
  filter: blur(30rpx);
  animation: pulse 4s ease-in-out infinite 1s;
}

@keyframes pulse {
  0%, 100% { opacity: 0.5; transform: scale(1); }
  50% { opacity: 0.8; transform: scale(1.1); }
}

/* 头部样式 */
.header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 32rpx;
  background: rgba(15, 23, 42, 0.95);
  backdrop-filter: blur(20rpx);
  border-bottom: 1rpx solid rgba(255, 255, 255, 0.1);
  position: sticky;
  top: 0;
  z-index: 100;
}

.back-btn {
  padding: 16rpx;
  border-radius: 12rpx;
  background: rgba(255, 255, 255, 0.1);
}

.back-icon {
  width: 24rpx;
  height: 24rpx;
}

.header-title {
  font-size: 32rpx;
  font-weight: 600;
  color: #f1f5f9;
}

.header-actions {
  display: flex;
  gap: 16rpx;
}

.action-icon {
  width: 32rpx;
  height: 32rpx;
  padding: 8rpx;
  border-radius: 8rpx;
  background: rgba(255, 255, 255, 0.1);
}

/* 项目分类 */
.project-categories {
  padding: 24rpx 32rpx;
  background: rgba(15, 23, 42, 0.8);
  border-bottom: 1rpx solid rgba(255, 255, 255, 0.1);
}

.category-scroll {
  white-space: nowrap;
}

.category-list {
  display: inline-flex;
  gap: 16rpx;
}

.category-item {
  padding: 16rpx 24rpx;
  background: rgba(30, 41, 59, 0.6);
  border: 1rpx solid rgba(255, 255, 255, 0.1);
  border-radius: 20rpx;
  font-size: 26rpx;
  color: #94a3b8;
  transition: all 0.3s ease;
}

.category-item.active {
  background: linear-gradient(135deg, #16a34a, #0d9488);
  color: white;
  border-color: transparent;
}

/* 项目网格 */
.projects-grid {
  padding: 32rpx;
  display: grid;
  grid-template-columns: 1fr;
  gap: 32rpx;
}

.project-card {
  background: rgba(30, 41, 59, 0.8);
  border: 1rpx solid rgba(255, 255, 255, 0.1);
  border-radius: 24rpx;
  overflow: hidden;
  transition: all 0.3s ease;
}

.project-card:active {
  transform: translateY(-2rpx);
  box-shadow: 0 8rpx 32rpx rgba(0, 0, 0, 0.3);
}

.project-cover {
  width: 100%;
  height: 200rpx;
  object-fit: cover;
}

.project-badge {
  position: absolute;
  top: 20rpx;
  right: 20rpx;
  padding: 8rpx 16rpx;
  border-radius: 12rpx;
  font-size: 20rpx;
  font-weight: 600;
  color: white;
}

.project-badge.active {
  background: linear-gradient(135deg, #16a34a, #0d9488);
}

.project-badge.completed {
  background: linear-gradient(135deg, #3b82f6, #2563eb);
}

.project-badge.planning {
  background: linear-gradient(135deg, #f59e0b, #d97706);
}

.project-content {
  padding: 24rpx;
}

.project-title {
  font-size: 32rpx;
  font-weight: 600;
  color: #f1f5f9;
  display: block;
  margin-bottom: 12rpx;
}

.project-desc {
  font-size: 26rpx;
  color: #94a3b8;
  line-height: 1.4;
  display: block;
  margin-bottom: 20rpx;
}

.project-tech {
  display: flex;
  flex-wrap: wrap;
  gap: 8rpx;
  margin-bottom: 20rpx;
}

.tech-tag {
  padding: 6rpx 12rpx;
  background: rgba(22, 163, 74, 0.1);
  border: 1rpx solid rgba(22, 163, 74, 0.2);
  border-radius: 8rpx;
  font-size: 20rpx;
  color: #16a34a;
}

.tech-more {
  padding: 6rpx 12rpx;
  background: rgba(255, 255, 255, 0.1);
  border-radius: 8rpx;
  font-size: 20rpx;
  color: #94a3b8;
}

.project-meta {
  display: flex;
  gap: 20rpx;
  margin-bottom: 20rpx;
}

.meta-item {
  display: flex;
  align-items: center;
  gap: 6rpx;
}

.meta-icon {
  width: 20rpx;
  height: 20rpx;
  opacity: 0.7;
}

.meta-text {
  font-size: 22rpx;
  color: #94a3b8;
}

.project-progress {
  margin-bottom: 20rpx;
}

.progress-bar {
  width: 100%;
  height: 8rpx;
  background: rgba(255, 255, 255, 0.1);
  border-radius: 4rpx;
  overflow: hidden;
  margin-bottom: 8rpx;
}

.progress-fill {
  height: 100%;
  background: linear-gradient(135deg, #16a34a, #0d9488);
  border-radius: 4rpx;
  transition: width 0.3s ease;
}

.progress-text {
  font-size: 22rpx;
  color: #16a34a;
  font-weight: 600;
}

.project-actions {
  display: flex;
  gap: 12rpx;
}

.btn-sm {
  padding: 12rpx 20rpx;
  font-size: 24rpx;
  border-radius: 12rpx;
  flex: 1;
}

/* 加载更多 */
.load-more {
  padding: 32rpx;
  text-align: center;
}

/* 空状态 */
.empty-state {
  padding: 80rpx 32rpx;
  text-align: center;
}

.empty-icon {
  width: 120rpx;
  height: 120rpx;
  margin-bottom: 24rpx;
  opacity: 0.5;
}

.empty-text {
  font-size: 32rpx;
  font-weight: 600;
  color: #f1f5f9;
  display: block;
  margin-bottom: 12rpx;
}

.empty-desc {
  font-size: 26rpx;
  color: #94a极速版;
  display: block;
}

/* 底部导航 */
.bottom-nav {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  background: rgba(15, 23, 42, 0.95);
  backdrop-filter: blur极速版);
  border-top: 1rpx solid rgba(255, 255, 255, 0.1);
  display: flex;
  justify-content: space-around;
  padding: 16rpx 0;
  z-index: 1000;
}

.nav-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 16极速版 24rpx;
}

.nav-item.active .nav-text {
  color: #16a34a;
}

.nav-icon {
  width: 44rpx;
  height: 44rpx;
  margin-bottom: 8rpx;
}

.nav-text {
  font-size: 20rpx;
  color: #94a3b8;
}

/* 模态框 */
.modal {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  z-index: 2000;
  display: none;
}

.modal.show {
  display: block;
}

.modal-overlay {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.7);
  backdrop-filter: blur(10rpx);
}

.modal-content {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 极速版;
  background: #0f172a;
  border-radius: 32rpx 32rpx 0 0;
  max-height: 80vh;
  animation: slideUp 0.3s ease;
}

@keyframes slideUp {
  from {
    transform: translateY(100%);
  }
  to {
    transform: translateY(0);
  }
}

.modal-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 32rpx;
  border-bottom: 1rpx solid rgba(255, 255, 255, 0.1);
}

.modal-title {
  font-size: 32rpx;
  font-weight: 600;
  color: #f1f5f9;
}

.modal-close {
  width: 32rpx;
  height: 32rpx;
  padding: 8rpx;
  border-radius: 8rpx;
  background: rgba(255, 255, 255, 0.1);
}

.modal-body {
  max-height: 60vh;
  padding: 32rpx;
}

/* 项目详情 */
.project-detail {
  text-align: center;
}

.detail-cover {
  width: 100%;
  height: 240rpx;
  border-radius: 16rpx;
  margin-bottom: 24rpx;
  object-fit: cover;
}

.detail-badge {
  display: inline-block;
  padding: 8rpx 16rpx;
  border-radius: 12rpx;
  font-size: 22rpx;
  font-weight: 600;
  color: white;
  margin-bottom: 16rpx;
}

.detail-title {
  font-size: 36rpx;
  font-weight: 700;
  color: #f1f5f9;
  display: block;
  margin-bottom: 8rpx;
}

.detail-date {
  font-size: 26rpx;
  color: #94a3b8;
  display: block;
  margin-bottom: 32rpx;
}

.detail-section {
  text-align: left;
  margin-bottom: 32rpx;
}

.section-title {
  font-size: 28rpx;
  font-weight: 600;
  color: #f1f5f9;
  display: block;
  margin-bottom: 16r极速版;
}

.section-content {
  font-size: 26rpx;
  color: #94a3b8;
  line-height: 1.6;
  display: block;
}

.tech-grid {
  display: flex;
  flex-wrap: wrap;
  gap: 12rpx;
}

.tech-tag.large {
  padding: 8rpx 16rpx;
  font-size: 24rpx;
}

.progress-container {
  display: flex;
  align-items: center;
  gap: 16rpx;
}

.progress-bar.large {
  flex: 1;
  height: 12rpx;
}

.progress-percent {
  font-size: 24rpx;
  font-weight: 600;
  color: #16a34a;
}

.team-members {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
}

.member-item {
  display: flex;
  align-items: center;
  gap: 16rpx;
  padding: 16rpx;
  background: rgba(30, 41, 59, 0.5);
  border-radius: 12rpx;
}

.member-avatar {
  width: 48rpx;
  height: 48rpx;
  border-radius: 8rpx;
}

.member-name {
  font-size: 26rpx;
  font-weight: 500;
  color: #f1f5f9;
}

.member-role {
  font-size: 22rpx;
  color: #94a3b8;
}

.achievements {
  display: flex;
  flex-direction: column;
  gap: 12rpx;
}

.achievement-item {
  display: flex;
  align-items: center;
  gap: 12rpx;
}

.achievement-icon {
  width: 20rpx;
  height: 20rpx;
}

.achievement-text {
  font-size: 24rpx;
  color: #94a3b8;
}

.project-links {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
}

.link-item {
  display: flex;
  align-items: center;
  padding: 20rpx;
  background: rgba(30, 41, 59, 0.5);
  border: 1rpx solid rgba(255, 255, 255, 0.1);
  border-radius: 12rpx;
}

.link-icon {
  width: 24rpx;
  height: 24rpx;
  margin-right: 16rpx;
}

.link-text {
  font-size: 26rpx;
  color: #94a3b8;
}

/* 响应式设计 */
@media (min-width: 768px) {
  .projects-grid {
    grid-template-columns: repeat(2, 1fr);
  }
  
  .modal-content {
    max-width: 600rpx;
    left: 50%;
    transform: translateX(-50%);
    border-radius: 32rpx;
    bottom: auto;
    top: 50%;
    transform: translate(-50%, -50%);
    max-height: 90vh;
  }
}

/* 安全区域适配 */
.safe-area-bottom {
  padding-bottom: constant(safe-area-inset-bottom);
  padding-bottom: env(safe-area-inset-bottom);
}

.safe-area-top {
  padding-top: constant(safe-area-inset-top);
  padding-top: env(safe-area-inset-top);
}